---
phase: 04-snooze-feature
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/Snooze.ts
  - src/services/dbService.ts
  - server.ts
autonomous: true
requirements:
  - SNOOZE-01
  - SNOOZE-02
  - SNOOZE-03
must_haves:
  truths:
    - "Snoozed emails are persisted in MongoDB"
    - "Snooze API endpoints work correctly"
    - "Users can view and manage snoozed emails"
  artifacts:
    - path: "src/models/Snooze.ts"
      provides: "Snooze Mongoose model"
    - path: "src/services/dbService.ts"
      provides: "Snooze CRUD methods"
    - path: "server.ts"
      provides: "Snooze API endpoints"
---

<objective>
Create backend model, database service extensions, and API endpoints for snoozing emails.
</objective>

<context>
@.planning/phases/04-snooze-feature/04-CONTEXT.md
@src/models/Filter.ts
@src/models/Rule.ts
@src/services/dbService.ts
@server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Snooze model</name>
  <files>src/models/Snooze.ts</files>
  <action>
Create Mongoose model for snoozed emails following the pattern in Filter.ts:

```typescript
// Snooze duration options
export type SnoozeDuration = 'later_today' | 'tomorrow' | 'next_week' | 'custom';

export interface ISnooze extends Document {
  userId: string;
  emailId: string;
  sender: string;
  subject: string;
  snoozedUntil: Date;
  originalAction?: 'delete' | 'skip';
  createdAt: Date;
}

export interface ISnoozeSettings extends Document {
  userId: string;
  defaultDuration: SnoozeDuration;
  customDuration?: number;
  notifyOnReturn: boolean;
}
```

Export Snooze and SnoozeSettings models.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Snooze model exists with proper schema and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Extend DbService with snooze methods</name>
  <files>src/services/dbService.ts</files>
  <read_first>
src/services/dbService.ts
src/models/Snooze.ts
  </read_first>
  <action>
Add to DbService class following existing patterns:

- `snoozeEmail(emailId, sender, subject, snoozedUntil, userId)` — Create snooze record
- `getSnoozedEmails(userId)` — Get all snoozed emails for user
- `unsnoozeEmail(snoozeId, userId)` — Remove snooze, return to inbox
- `getEmailsDueForReturn(userId)` — Get emails whose snooze has expired
- `updateSnoozeSettings(userId, settings)` — Update user preferences
- `getSnoozeSettings(userId)` — Get user preferences
  </action>
  <verify>
  </verify>
  <done>DbService has all snooze CRUD methods</done>
</task>

<task type="auto">
  <name>Task 3: Add API endpoints for snooze</name>
  <files>server.ts</files>
  <read_first>
server.ts
  </read_first>
  <action>
Add snooze endpoints:

```
POST /api/snooze - Snooze an email
GET /api/snooze - Get all snoozed emails
DELETE /api/snooze/:id - Unsnooze (return to inbox)
POST /api/snooze/:id/extend - Extend snooze duration
GET /api/snooze/due - Get emails due for return
GET /api/snooze/settings - Get snooze preferences
PUT /api/snooze/settings - Update snooze preferences
```

Use getUserId(req) for user identification.
  </action>
  <verify>
  </verify>
  <done>All snooze API endpoints exist</done>
</task>

</tasks>

<verification>
- [ ] Snooze model created in MongoDB
- [ ] DbService has snooze methods
- [ ] API endpoints return proper JSON
</verification>

<success_criteria>
Backend supports snoozing emails with various durations and returning them at specified times
</success_criteria>

<output>
After completion, create `.planning/phases/04-snooze-feature/04-01-SUMMARY.md`
</output>
