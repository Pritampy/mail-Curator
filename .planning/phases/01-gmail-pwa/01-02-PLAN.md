---
phase: 01-gmail-pwa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [server.ts, src/services/gmailService.ts, src/services/dbService.ts, src/models/EmailAction.ts]
autonomous: true
requirements: [GMAIL-05, GMAIL-06]
user_setup:
  - service: mongodb
    why: "Persistent storage for email actions"
    env_vars:
      - name: MONGODB_URI
        source: "MongoDB Atlas Dashboard -> Connect -> Connection String"
    dashboard_config:
      - task: "Create database"
        location: "MongoDB Atlas -> Database -> Create Database"
      - task: "Create email_actions collection"
        location: "MongoDB Atlas -> Collections -> Create Collection"

must_haves:
  truths:
    - "Email actions persist in MongoDB"
    - "REST endpoints work correctly"
    - "Stats aggregate from MongoDB"
    - "Gmail API integration works"
  artifacts:
    - path: "server.ts"
      provides: "Express server with API routes"
      min_lines: 200
    - path: "src/services/gmailService.ts"
      provides: "Gmail API operations"
      exports: ["getEmails", "deleteEmail", "untrashEmail"]
    - path: "src/services/dbService.ts"
      provides: "MongoDB operations"
      exports: ["saveAction", "getActions", "getStats"]
    - path: "src/models/EmailAction.ts"
      provides: "Mongoose schema"
      fields: ["emailId", "sender", "subject", "action", "timestamp", "userId"]
  key_links:
    - from: "server.ts"
      to: "gmailService"
      via: "import and function calls"
      pattern: "import.*gmailService"
    - from: "server.ts"
      to: "dbService"
      via: "import and function calls"
      pattern: "import.*dbService"
    - from: "dbService"
      to: "MongoDB"
      via: "mongoose connection"
      pattern: "mongoose.connect"
---

<objective>
Build modular Node.js backend with Gmail API integration and MongoDB Atlas persistence.

Purpose: Replace in-memory session storage with MongoDB, create modular service architecture, implement REST endpoints for email processing and stats.
Output: Production-ready backend with GmailService, DbService, and REST API
</objective>

<context>
@server.ts
@.planning/phases/01-gmail-pwa/01-CONTEXT.md

<interfaces>
<!-- Key interfaces -->
```typescript
// EmailAction model
interface EmailAction {
  emailId: string;
  sender: string;
  subject: string;
  action: 'delete' | 'skip';
  timestamp: Date;
  userId: string;
}

// Stats aggregation
interface Stats {
  totalProcessed: number;
  totalDeleted: number;
  totalSkipped: number;
  efficiency: number;
  activity: Array<{ _id: string; count: number }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmailAction Mongoose model</name>
  <files>src/models/EmailAction.ts</files>
  <action>
    Create src/models/EmailAction.ts:
    1. Define mongoose schema with fields: emailId (string, required), sender (string), subject (string), action (enum: 'delete', 'skip'), timestamp (Date, default: now), userId (string)
    2. Add index on timestamp for efficient queries
    3. Add compound index on userId + timestamp for per-user stats
    4. Export as EmailAction model
  </action>
  <verify>
    <automated>grep -n "mongoose.model" src/models/EmailAction.ts</automated>
  </verify>
  <done>EmailAction model created with proper indexes</done>
</task>

<task type="auto">
  <name>Task 2: Create GmailService for API operations</name>
  <files>src/services/gmailService.ts</files>
  <action>
    Create src/services/gmailService.ts:
    1. Create GmailService class with oauth2Client dependency
    2. Method getEmails(userId, maxResults=20): fetch emails from Gmail API with subject, from, date, snippet
    3. Method deleteEmail(emailId): trash email via gmail.users.messages.trash
    4. Method untrashEmail(emailId): untrash email via gmail.users.messages.untrash
    5. Use existing googleapis library (already installed)
    6. Return typed Email[] objects
  </action>
  <verify>
    <automated>grep -n "class GmailService" src/services/gmailService.ts</automated>
  </verify>
  <done>GmailService with getEmails, deleteEmail, untrashEmail methods</done>
</task>

<task type="auto">
  <name>Task 3: Create DbService for MongoDB operations</name>
  <files>src/services/dbService.ts</files>
  <action>
    Create src/services/dbService.ts:
    1. Create DbService class with mongoose connection
    2. Method saveAction(action: EmailAction): save to MongoDB
    3. Method getActions(userId): get all actions for user
    4. Method getStats(userId): aggregate stats - totalProcessed, totalDeleted, totalSkipped, efficiency, activity (last 7 days)
    5. Use Mongoose aggregation pipeline for stats
    6. Handle connection errors gracefully
  </action>
  <verify>
    <automated>grep -n "class DbService" src/services/dbService.ts</automated>
  </done>
</task>

<task type="auto">
  <name>Task 4: Update server.ts with REST endpoints and services</name>
  <files>server.ts</files>
  <read_first>server.ts</read_first>
  <action>
    Update server.ts to:
    1. Add mongoose import and connect to MONGODB_URI
    2. Import GmailService and DbService
    3. Instantiate services after OAuth setup
    4. Update GET /api/emails to use GmailService.getEmails
    5. Create POST /api/delete: use GmailService.deleteEmail + DbService.saveAction
    6. Create POST /api/skip: use DbService.saveAction (no Gmail action)
    7. Update GET /api/stats to use DbService.getStats
    8. Handle errors with proper status codes (400, 401, 500)
    9. Keep existing auth endpoints unchanged
  </action>
  <verify>
    <automated>grep -n "POST /api/delete\|POST /api/skip" server.ts</automated>
  </verify>
  <done>REST endpoints connected to modular services with MongoDB persistence</done>
</task>

<task type="auto">
  <name>Task 5: Add MongoDB URI to .env.example</name>
  <files>.env.example</files>
  <read_first>.env.example</read_first>
  <action>
    Add to .env.example:
    ```
    # MongoDB Atlas
    MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/kinetic?retryWrites=true&w=majority
    ```
  </action>
  <verify>
    <automated>grep "MONGODB_URI" .env.example</automated>
  </verify>
  <done>MONGODB_URI added to .env.example</done>
</task>

</tasks>

<verification>
- [ ] GET /api/emails returns email list from Gmail
- [ ] POST /api/delete trashes email in Gmail + saves to MongoDB
- [ ] POST /api/skip saves action to MongoDB only
- [ ] GET /api/stats returns aggregated stats from MongoDB
- [ ] Services are modular and separated
- [ ] Error handling returns proper status codes
</verification>

<success_criteria>
Backend has modular architecture with GmailService and DbService. All email actions persist to MongoDB. Stats aggregate correctly from database.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gmail-pwa/01-02-SUMMARY.md`
</output>
