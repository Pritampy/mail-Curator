---
phase: 01-gmail-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx, src/components/CardStack.tsx, src/types.ts]
autonomous: true
requirements: [GMAIL-01, GMAIL-02, GMAIL-03, GMAIL-04, GMAIL-07]
user_setup: []

must_haves:
  truths:
    - "User can swipe right to delete email"
    - "User can swipe left to skip email"
    - "UI updates immediately on swipe (optimistic)"
    - "User can undo last action"
    - "Next batch loads automatically"
    - "Loading states shown during API calls"
    - "Error states shown on failure"
  artifacts:
    - path: "src/components/CardStack.tsx"
      provides: "Swipe gesture handling and card rendering"
      min_lines: 150
    - path: "src/App.tsx"
      provides: "State management, API calls, undo logic"
      min_lines: 150
    - path: "src/types.ts"
      provides: "TypeScript interfaces"
      contains: "Email, Stats, User"
  key_links:
    - from: "CardStack.tsx"
      to: "App.tsx"
      via: "onAction callback"
      pattern: "onAction\\(emailId, action"
    - from: "App.tsx"
      to: "/api/emails"
      via: "fetch in useEffect"
      pattern: "fetch\\('/api/emails'\\)"
    - from: "App.tsx"
      to: "/api/delete or /api/skip"
      via: "POST fetch"
      pattern: "fetch\\('/api/(delete|skip)'"
---

<objective>
Enhance frontend swipe logic, card state management, optimistic updates, and undo functionality.

Purpose: Connect existing CardStack UI with backend APIs, implement optimistic updates with rollback, and add proper loading/error states.
Output: Working swipe-to-action flow with 100ms threshold, optimistic UI, and undo
</objective>

<context>
@src/App.tsx
@src/components/CardStack.tsx
@src/types.ts
@.planning/phases/01-gmail-pwa/01-CONTEXT.md

<interfaces>
<!-- Key interfaces from existing codebase -->
From src/types.ts:
```typescript
interface Email {
  id: string;
  threadId?: string;
  subject: string;
  from: string;
  snippet?: string;
  date: string;
}

interface Stats {
  totalProcessed: number;
  totalDeleted: number;
  totalSkipped: number;
  efficiency: number;
  activity: Array<{ _id: string; count: number }>;
}

interface User {
  id: string;
  email: string;
  name: string;
  picture?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CardStack swipe threshold and API calls</name>
  <files>src/components/CardStack.tsx</files>
  <read_first>src/components/CardStack.tsx</read_first>
  <action>
    Update CardStack.tsx to:
    1. Change swipe threshold from 100 to 100 (keep existing 100px threshold)
    2. Modify handleSwipe to call separate endpoints: right swipe → POST /api/delete, left swipe → POST /api/skip
    3. Keep existing visual feedback (rotation, overlay colors: purple for delete, gray for skip)
    4. Ensure stack displays up to 3 cards with proper z-index and scale reduction
    5. Keep existing AnimatePresence mode="popLayout" for smooth transitions
  </action>
  <verify>
    <automated>grep -n "handleSwipe" src/components/CardStack.tsx | head -5</automated>
  </verify>
  <done>Swipe triggers correct API endpoint (delete for right, skip for left) with 100px threshold</done>
</task>

<task type="auto">
  <name>Task 2: Implement optimistic updates with rollback in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
    Update App.tsx handleAction to:
    1. Optimistically remove email from state immediately using setEmails(prev => prev.filter(...))
    2. Store lastAction with emailId, action type, and full emailData for undo
    3. Make async POST call to /api/delete or /api/skip
    4. On API failure: restore email to state and show error toast
    5. On success: fetch updated stats via fetchStats()
    6. Use try/catch with proper error handling - console.error on failure
  </action>
  <verify>
    <automated>grep -n "handleAction" src/App.tsx | head -5</automated>
  </verify>
  <done>Optimistic update removes card immediately, rolls back on API failure</done>
</task>

<task type="auto">
  <name>Task 3: Implement single-level undo functionality</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
    Update App.tsx handleUndo to:
    1. Check if lastAction exists (non-null)
    2. Restore email to beginning of emails array: setEmails(prev => [lastAction.data, ...prev])
    3. Clear lastAction: setLastAction(null)
    4. Note: For delete actions, backend should handle untrash if undo is called (this is backend task)
    5. Disable undo button when lastAction is null (use canUndo prop)
  </action>
  <verify>
    <automated>grep -n "handleUndo" src/App.tsx | head -5</automated>
  </verify>
  <done>Undo restores last deleted/skipped email to top of stack</done>
</task>

<task type="auto">
  <name>Task 4: Implement email batch preloading</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
    Update App.tsx fetchEmails to:
    1. Load 20 emails per batch (keep existing maxResults: 20)
    2. Add preloading check: when emails.length <= 5, trigger next batch fetch
    3. Use useEffect with dependency on emails.length to trigger preload
    4. Append new emails to existing list (avoid duplicates by checking email.id)
    5. Show loading spinner during preload fetch
    6. Handle empty inbox case (show "Inbox Zero" message)
  </action>
  <verify>
    <automated>grep -n "fetchEmails" src/App.tsx | head -5</automated>
  </verify>
  <done>Next batch loads automatically when 5 or fewer emails remaining</done>
</task>

<task type="auto">
  <name>Task 5: Add loading and error states</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
    Update App.tsx to:
    1. Keep existing loading state with spinner (already exists)
    2. Add error state: const [error, setError] = useState<string | null>(null)
    3. Show error toast/alert on API failures in handleAction (setError, clear after 3s)
    4. Handle auth errors (401): prompt re-authentication via handleLogin
    5. Show inline error message in CardStack when email fetch fails
    6. Add retry button for failed API calls
  </action>
  <verify>
    <automated>grep -n "error" src/App.tsx | head -10</automated>
  </verify>
  <done>Loading spinner during API calls, error toast on failure, auth prompt on 401</done>
</task>

</tasks>

<verification>
- [ ] Swipe right → POST /api/delete with {emailId}
- [ ] Swipe left → POST /api/skip with {emailId}
- [ ] Card removed immediately on swipe (optimistic)
- [ ] Undo button restores last email
- [ ] Next batch loads at 5 remaining
- [ ] Loading spinner visible during fetches
- [ ] Error displayed on API failure
</verification>

<success_criteria>
Email processing flow complete: user can swipe to delete/skip with immediate feedback, undo last action, and automatic batch preloading. Error states handle API failures gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gmail-pwa/01-01-SUMMARY.md`
</output>
