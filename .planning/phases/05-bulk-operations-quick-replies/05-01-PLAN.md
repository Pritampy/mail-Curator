---
phase: 05-bulk-operations-quick-replies
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/QuickReply.ts
  - src/services/dbService.ts
  - server.ts
autonomous: true
requirements:
  - BULK-01
  - BULK-02
  - QUICKREPLY-01
must_haves:
  truths:
    - "Quick reply templates are persisted"
    - "Bulk operations API endpoints work"
  artifacts:
    - path: "src/models/QuickReply.ts"
      provides: "QuickReply Mongoose model"
    - path: "src/services/dbService.ts"
      provides: "Quick reply CRUD methods"
    - path: "server.ts"
      provides: "Quick reply and bulk API endpoints"
---

<objective>
Create backend model, database service extensions, and API endpoints for quick reply templates and bulk operations.
</objective>

<context>
@.planning/phases/05-bulk-operations-quick-replies/05-CONTEXT.md
@src/models/Filter.ts
@src/services/dbService.ts
@server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickReply model</name>
  <files>src/models/QuickReply.ts</files>
  <action>
Create Mongoose model for quick reply templates:

```typescript
export interface IQuickReply extends Document {
  userId: string;
  name: string;
  template: string;
  createdAt: Date;
  updatedAt: Date;
}
```

Export QuickReply model.
  </action>
  <verify>
  </verify>
  <done>QuickReply model exists with proper schema</done>
</task>

<task type="auto">
  <name>Task 2: Extend DbService with quick reply methods</name>
  <files>src/services/dbService.ts</files>
  <action>
Add to DbService:

- `createQuickReply(userId, name, template)` — Create template
- `getQuickReplies(userId)` — List all templates
- `updateQuickReply(id, userId, updates)` — Update template
- `deleteQuickReply(id, userId)` — Delete template

Import QuickReply model.
  </action>
  <verify>
  </verify>
  <done>DbService has quick reply CRUD methods</done>
</task>

<task type="auto">
  <name>Task 3: Add API endpoints for quick replies and bulk actions</name>
  <files>server.ts</files>
  <action>
Add endpoints:

```
# Quick Replies
GET /api/quickreplies - List templates
POST /api/quickreplies - Create template
PATCH /api/quickreplies/:id - Update template
DELETE /api/quickreplies/:id - Delete template

# Bulk Operations
POST /api/bulk/delete - Bulk delete emails
POST /api/bulk/archive - Bulk archive emails
POST /api/bulk/skip - Bulk skip emails
POST /api/bulk/label - Bulk apply label
```

For bulk operations, receive array of emailIds.
  </action>
  <verify>
  </verify>
  <done>All quick reply and bulk API endpoints exist</done>
</task>

</tasks>

<verification>
- [x] QuickReply model created
- [x] DbService has quick reply methods
- [x] Bulk operation endpoints exist
</verification>

<success_criteria>
Backend supports quick reply templates and bulk email operations
</success_criteria>

<output>
After completion, create `.planning/phases/05-bulk-operations-quick-replies/05-01-SUMMARY.md`
</output>
