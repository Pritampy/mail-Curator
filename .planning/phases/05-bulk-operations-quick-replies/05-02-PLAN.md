---
phase: 05-bulk-operations-quick-replies
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/BulkSelectMode.tsx
  - src/components/QuickReplyList.tsx
  - src/components/QuickReplyEditor.tsx
  - src/types.ts
autonomous: true
requirements:
  - BULK-01
  - BULK-02
  - QUICKREPLY-01
must_haves:
  truths:
    - "Users can select multiple emails"
    - "Users can perform bulk actions"
    - "Users can manage quick reply templates"
  artifacts:
    - path: "src/components/BulkSelectMode.tsx"
      provides: "Bulk selection and action UI"
    - path: "src/components/QuickReplyList.tsx"
      provides: "Quick reply template management"
---

<objective>
Create frontend UI components for bulk operations and quick reply template management.
</objective>

<context>
@.planning/phases/05-bulk-operations-quick-replies/05-01-SUMMARY.md
@src/components/CardStack.tsx
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QuickReply types</name>
  <files>src/types.ts</files>
  <action>
Add to src/types.ts:

```typescript
export interface QuickReply {
  _id: string;
  userId: string;
  name: string;
  template: string;
  createdAt: string;
  updatedAt: string;
}
```
  </action>
  <verify>
  </verify>
  <done>QuickReply type added</done>
</task>

<task type="auto">
  <name>Task 2: Create BulkSelectMode component</name>
  <files>src/components/BulkSelectMode.tsx</files>
  <action>
Create bulk selection UI:

- Checkbox overlay on email cards for selection
- "Select all" button
- Selection counter
- Action bar with: Delete, Archive, Skip, Apply Label buttons
- Cancel/clear selection button

Show when user enters bulk mode (long press or button).
  </action>
  <verify>
  </verify>
  <done>Bulk selection UI works</done>
</task>

<task type="auto">
  <name>Task 3: Create QuickReplyList component</name>
  <files>src/components/QuickReplyList.tsx</files>
  <action>
Create quick reply template management:

- List all templates with name and preview
- Add new template button
- Edit/delete actions per template
- Empty state when no templates

Fetch from GET /api/quickreplies
  </action>
  <verify>
  </verify>
  <done>Quick reply list displays</done>
</task>

<task type="auto">
  <name>Task 4: Create QuickReplyEditor component</name>
  <files>src/components/QuickReplyEditor.tsx</files>
  <action>
Create modal for editing quick reply templates:

- Name input
- Template textarea (multiline)
- Save and Cancel buttons
- Validation: name and template required
  </action>
  <verify>
  </verify>
  <done>Quick reply editor modal works</done>
</task>

<task type="auto">
  <name>Task 5: Integrate into existing components</name>
  <files>src/components/Navigation.tsx, src/App.tsx</files>
  <action>
Add Quick Replies tab to Navigation.
Integrate BulkSelectMode into CardStack or App.
Add quick reply handlers to App.tsx.
  </action>
  <verify>
  </verify>
  <done>Bulk and quick reply UI integrated</done>
</task>

</tasks>

<verification>
- [x] Users can select multiple emails
- [x] Bulk actions work
- [x] Quick reply templates can be managed
</verification>

<success_criteria>
Frontend supports bulk operations and quick reply template management
</success_criteria>

<output>
After completion, create `.planning/phases/05-bulk-operations-quick-replies/05-02-SUMMARY.md`
</output>
