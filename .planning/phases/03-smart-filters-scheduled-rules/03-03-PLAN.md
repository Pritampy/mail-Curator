---
phase: 03-smart-filters-scheduled-rules
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/RulesList.tsx
  - src/components/RuleEditor.tsx
  - src/components/Navigation.tsx
  - src/App.tsx
autonomous: true
requirements:
  - RULES-01
  - RULES-02
user_setup: []
must_haves:
  truths:
    - "Users can view list of scheduled rules"
    - "Users can create rules with conditions and schedules"
    - "Users can edit existing rules"
    - "Users can enable/disable rules"
    - "Users can manually trigger rule execution"
    - "Users can view execution history"
  artifacts:
    - path: "src/components/RulesList.tsx"
      provides: "Rules list display component"
    - path: "src/components/RuleEditor.tsx"
      provides: "Rule create/edit modal"
  key_links:
    - from: "RulesList.tsx"
      to: "/api/rules"
      via: "fetch GET/POST/PATCH/DELETE"
    - from: "RulesList.tsx"
      to: "/api/rules/:id/execute"
      via: "fetch POST for manual run"
    - from: "Navigation.tsx"
      to: "RulesList.tsx"
      via: "activeTab state"
---

<objective>
Create frontend UI components for viewing and managing scheduled cleanup rules.
</objective>

<context>
@.planning/phases/03-smart-filters-scheduled-rules/03-01-SUMMARY.md
@src/components/FilterList.tsx
@src/components/FilterEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RulesList component</name>
<files>src/components/RulesList.tsx</files>
<action>
Create RulesList component following FilterList styling patterns:

- Header with "Scheduled Rules" title and "Add Rule" button
- List of rule cards showing:
  - Rule name
  - Schedule summary (e.g., "Daily at 3:00 PM", "Weekly on Monday")
  - Action description (e.g., "Delete emails older than 30 days")
  - Next run date/time
  - Last run status (success/failure with count)
  - Enabled toggle
- Empty state when no rules exist
- Each rule card has edit/delete/toggle actions
- Run now button for manual execution
- Loading and error states

Use Tailwind classes matching existing design system.
  </action>
  <verify>
  </verify>
  <done>RulesList renders rules from API, shows schedule info, handles loading/error</done>
</task>

<task type="auto">
  <name>Task 2: Create RuleEditor component</name>
<files>src/components/RuleEditor.tsx</files>
  <action>
Create modal/form for creating and editing scheduled rules:

- Rule name input
- Conditions builder (same as FilterEditor - reuse logic if possible):
  - Add condition button
  - Each condition: field, operator, value inputs
  - Remove condition button
- Action dropdown: delete, archive, skip, apply label
- Label input (if action is apply label)
- Schedule configuration:
  - Type dropdown: daily, weekly, monthly
  - Time picker (HH:mm format)
  - Day of week dropdown (for weekly)
  - Day of month input (for monthly)
- Enable toggle
- Save and Cancel buttons
- Form validation

Use modal overlay with existing design patterns.
  </action>
  <verify>
  </verify>
  <done>RuleEditor modal opens, validates input, submits to API, calculates next run</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Rules into Navigation and App</name>
<files>src/components/Navigation.tsx, src/App.tsx</files>
<action>
Add "Rules" tab to navigation:

- Add Settings icon or Clock icon from lucide-react
- Add Rules tab button
- Update BottomNav interface to accept 'rules' tab

In App.tsx:
- Update activeTab union type: 'inbox' | 'stats' | 'filters' | 'rules'
- Add RulesList to render when rules tab active
- Import and render RuleEditor as modal when editing/creating rule
- Handle navigation between all four tabs
  </action>
  <verify>
  </verify>
  <done>Rules tab appears in navigation, clicking shows RulesList component</done>
</task>

</tasks>

<verification>
Frontend builds without errors, rules tab navigable, rules can be created/edited/deleted/executed.
</verification>

<success_criteria>
- Users can view list of scheduled cleanup rules
- Users can create rules with conditions, actions, and schedules
- Users can edit existing rules
- Users can enable/disable rules
- Users can manually trigger rule execution
- Users can view execution history
- Rules UI matches existing design patterns
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-filters-scheduled-rules/03-03-SUMMARY.md`
</output>
